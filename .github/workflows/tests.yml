name: Tests

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install development dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]"

      - name: Run Ruff
        run: |
          ruff check .
          ruff format --check .

      - name: Run mypy
        run: python -m mypy

  pytest:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.13"]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install package and test dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[test]"

      - name: Run tests
        run: python -m pytest -q -W error

  wheel-smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Build wheel
        run: |
          python -m pip install --upgrade pip build
          python -m build --wheel

      - name: Verify wheel contents
        run: |
          python - <<'PY'
          from pathlib import Path
          from zipfile import ZipFile

          wheels = list(Path("dist").glob("*.whl"))
          if len(wheels) != 1:
              raise SystemExit(f"Expected one wheel, found: {wheels}")

          with ZipFile(wheels[0]) as archive:
              names = set(archive.namelist())

          source_root = Path("src/jerrythomas/templates")
          expected_templates = {
              f"jerrythomas/templates/{path.relative_to(source_root).as_posix()}"
              for path in source_root.rglob("*")
              if path.is_file()
              and path.suffix != ".pyc"
              and path.name != ".DS_Store"
          }
          packaged_templates = {
              name
              for name in names
              if name.startswith("jerrythomas/templates/")
              and not name.endswith("/")
          }
          missing = expected_templates - packaged_templates
          unexpected = packaged_templates - expected_templates
          if missing or unexpected:
              raise SystemExit(
                  "Wheel template inventory differs from source: "
                  f"missing={sorted(missing)}, unexpected={sorted(unexpected)}"
              )

          junk = sorted(
              name
              for name in names
              if "__pycache__" in name
              or name.endswith(".pyc")
              or name.endswith(".pyo")
              or name.endswith(".DS_Store")
          )
          if junk:
              raise SystemExit(f"Wheel contains generated files: {junk}")

          if not any(name.startswith("jerrythomas/") for name in names):
              raise SystemExit("Wheel does not contain the jerrythomas package")
          legacy = sorted(name for name in names if name.startswith("datapipeline/"))
          if legacy:
              raise SystemExit(f"Wheel contains the removed datapipeline package: {legacy}")
          PY

      - name: Smoke test installed wheel
        run: |
          python -m pip install --force-reinstall dist/*.whl
          python - <<'PY'
          import jerrythomas

          try:
              import datapipeline
          except ModuleNotFoundError:
              pass
          else:
              raise SystemExit(f"Legacy datapipeline package is importable: {datapipeline}")
          PY
          jerry --help
          smoke_root="$(mktemp -d)"
          cd "$smoke_root"
          jerry plugin create smoke-plugin --out .
          test -f smoke-plugin/jerry.yaml
          test -f smoke-plugin/your-project/.env.example
          test -f smoke-plugin/your-project/project.yaml
          test -f smoke-plugin/your-project/datasets/default.yaml
          test -f smoke-plugin/your-project/operations/dataset.yaml
          test -f smoke-plugin/your-project/operations/coverage.yaml
          test -f smoke-plugin/your-project/operations/matrix.yaml
          test ! -e smoke-plugin/your-project/dataset.yaml
          test ! -e smoke-plugin/your-dataset
          test ! -e smoke-plugin/your-project/profiles/build.schema.yaml
          test -f smoke-plugin/your-project/profiles/serve.defaults.yaml
          test -f smoke-plugin/your-project/profiles/serve.dataset.yaml
          test ! -e smoke-plugin/your-interim-data-builder
          test ! -e smoke-plugin/your-project/tasks
          test ! -e smoke-plugin/_dataset_base
          jerry demo create --out .
          test -f demo/jerry.yaml
          test -f demo/demo/.env.example
          test -f demo/demo/datasets/default.yaml
          test -f demo/demo/scripts/run_model_batches.py
          test ! -e demo/_dataset_base
          test ! -e jerry.yaml
          jerry list datasets --project demo/demo/project.yaml
          jerry list profiles --project demo/demo/project.yaml
          if find smoke-plugin demo \( -name __pycache__ -o -name '*.pyc' -o -name '.DS_Store' \) | grep -q .; then
            echo "Scaffolded output contains generated files"
            exit 1
          fi
