# Defaults for the build command and its build.<name>.yaml profiles.
# precedence: CLI > build.<name>.yaml > build.defaults.yaml > built-ins
# artifact_mode: auto
observability:
  visuals: true
  heartbeat_interval_seconds: 60
  logging:
    level: INFO
    outputs:
      - transport: STDERR
