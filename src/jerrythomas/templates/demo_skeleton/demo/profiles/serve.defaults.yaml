# Defaults for the serve command and its serve.<name>.yaml profiles.
# precedence: CLI > serve.<name>.yaml > serve.defaults.yaml > built-ins
# artifact_mode: AUTO
output:
  transport: fs
  format: jsonl
  # compression: gzip
  directory: ./output
observability:
  visuals: ON
  heartbeat_interval_seconds: 60
  logging:
    level: INFO
    outputs:
      - transport: STDERR
      - transport: FS
        scope: EXECUTION
