id: synthetic.ticks

parser:
  entrypoint: "core.temporal_record"
  args: {}
loader:
  entrypoint: "core.synthetic.ticks"
  args:
    start: "${start_time}"
    end: "${end_time}"
    frequency: "${cadence}"
freshness: opaque # synthetic data has no trackable input files
