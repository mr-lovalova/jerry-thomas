schema_version: 7
artifact_revision: 1
name: identity_alignment_project
paths:
  streams: streams
  sources: sources
  datasets: ./datasets
  operations: ./operations
  artifacts: build
  profiles: profiles
