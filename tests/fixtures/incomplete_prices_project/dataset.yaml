sample:
  rounding: ceil
  cadence: 1h

features:
  - id: spot_eur_scaled
    stream: metrics.elspot.eur
    field: value
    scale: true
  - id: spot_eur_sequence
    stream: metrics.elspot.eur
    field: value
    sequence:
      size: 3
      stride: 2

targets: []

postprocess:
  features:
    threshold: 0.4
