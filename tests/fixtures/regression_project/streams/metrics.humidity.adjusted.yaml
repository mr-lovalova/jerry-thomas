id: metrics.humidity.adjusted
from:
  stream: metrics.humidity
join:
  kind: broadcast
  with: metrics.linear

combine:
  entrypoint: combine_humidity_with_baseline
  args: {}

transforms:
  - { operation: rolling_slope, x: baseline, y: humidity, window: 2, to: slope }
