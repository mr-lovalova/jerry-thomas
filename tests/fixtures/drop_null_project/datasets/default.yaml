version: v1
sample:
  rounding: ceil
  cadence: 1h

features:
  - id: time_linear
    stream: time.ticks.linear
    field: value
    scale: false

targets: []

postprocess:
  features:
    threshold: 1.0
