id: synthetic.ticks

parser:
  entrypoint: "core.temporal_record"
  args: {}
loader:
  entrypoint: "core.synthetic.ticks"
  args:
    start: "${start_time}"
    end: "${end_time}"
    frequency: "2h"
