id: synthetic.ticks

parser:
  entrypoint: "core.record"
  args: {}
loader:
  entrypoint: "core.synthetic.ticks"
  args:
    start: "${start_time}"
    end: "${end_time}"
    frequency: "2h"
freshness: opaque
