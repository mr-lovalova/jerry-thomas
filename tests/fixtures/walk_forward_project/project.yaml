schema_version: 7
artifact_revision: 1
name: walk_forward_regression
paths:
  streams: streams
  sources: sources
  datasets: ./datasets
  operations: ./operations
  artifacts: artifacts
  profiles: profiles
