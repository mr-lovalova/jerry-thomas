artifact_mode: rebuild
output:
  transport: fs
  format: jsonl
  directory: output
observability:
  visuals: false
  heartbeat_interval_seconds: 0
  logging:
    level: CRITICAL
    outputs:
      - transport: STDERR
